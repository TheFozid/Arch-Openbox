-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html
--
-- Conky Lua API: https://conky.cc/lua

-- Configuration settings: https://conky.cc/config_settings
conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 0,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'Source Code Pro:size=10',
    gap_x = 0,
    gap_y = -2,
    minimum_height = 5,
    minimum_width = 1920,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = false,
    out_to_x = true,
    own_window = true,
    own_window_argb_visual = true,
    own_window_argb_value = 60,
    own_window_class = 'conky',
    own_window_colour = black,
    own_window_type = 'dock',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    own_window_transparent = false,
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

-- Variables: https://conky.cc/variables
conky.text = [[
archlinux | $uptime | RAM $memperc% of $memmax | $cpu% Load @ $freq_g}GHz | Processes $processes($running_processes) | / ${fs_used}/$fs_size /D ${fs_used /home/danny/D}/${fs_size /home/danny/D} /W ${fs_used /home/danny/Windows}/${fs_size /home/danny/Windows} /S ${fs_used /home/danny/Seagate}/${fs_size /home/danny/Seagate} | CPU ${hwmon 3 temp 2}°C • GPU ${hwmon 1 temp 1}°C | ${upspeed eth0}↑ • ${downspeed eth0}↓
]]

